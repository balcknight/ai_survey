
## 数据处理
/data/work/zhurui/survey_rec/data/survey 信息表.xlsx
sheet:处理后信息

第一列是文件路径信息:
例如:
/TJPROJ12/RESEQ/User/shenshaoqi/survey/X101SC2501/X101SC25015691-Z01-F009/CNST1239_CNST1239
截取 X101SC2501/X101SC25015691-Z01-F009/CNST1239_CNST1239
拼接 /data/work/zhurui/survey_rec/data/shenshaoqi_data/survey1
得到真实路径:/data/work/zhurui/survey_rec/data/shenshaoqi_data/survey1/X101SC2501/X101SC25015691-Z01-F009/CNST1239_CNST1239
(base) [zhurui@novoagi02 13:39:24 ~/.claude]
$ls /data/work/zhurui/survey_rec/data/shenshaoqi_data/survey1/X101SC2501/X101SC25015691-Z01-F009/CNST1239_CNST1239
all.ntcls.xls                   CNST1239.17merFreq.SpeFreq.cut  GC.pdf
all.ntspe.xls                   CNST1239.17merFreq.spe.pdf      X101SC25015691-Z01-F009.CNST1239.Survey_Report.zip
CNST1239.17merFreq.NumFreq.cut  CNST1239.pos
CNST1239.17merFreq.num.pdf      CNST1239.Result.xls


'是否流转-结果' 列
不属于['是', '否']的值，统一删除所在行.


## 判定规则

### k-mer图判定规则

**输入**：
1.SpeFreq.cut，有效范围 depth 3~300
2.NumFreq.cut(SpeFreq x 深度)，depth 3~300，供比值判定用


思路:用 11 点平滑掉小于 10 范围内的抖动，再用 distance=10 防止残留重复检峰。
window很像一个”低通滤波器”：
window 越大 → 截止更低 → 越”糊” → 小结构更容易消失（极端情况，window=1，所有小凸起都不变）

1. Savitzky-Golay 平滑（window=11，poly=3），消除低深度端噪声和宽峰顶部抖动
2. `scipy.signal.find_peaks` 检测极大值：
   - 仅用 `distance ≥ 10` 宽松找所有候选峰
   - 再过滤：保留高度 ≥ 候选峰中最高峰 × `prominence_ratio`（默认5%）的峰
   - `prominence_ratio`：峰高度相对于最高峰的最小比例阈值，用于过滤噪声峰
     - 值越小（如0.01=1%）→ 保留更多小峰，可能包含噪声
     - 值越大（如0.1=10%）→ 只保留显著峰，可能漏掉真实小峰
   - 阈值基于峰自身相对高度，不受样本绝对频率量级影响，对不同测序深度样本更鲁棒
3. 峰型异常检测：
   - 检查主峰（最高峰）左侧最低点是否过高
   - 计算主峰左侧最低点占峰高度的比例：left_min / main_peak_freq
   - spe 阈值：比例 > 0.33 则异常
   - num 阈值：比例 > 0.4 则异常
   - 异常峰型特征：左侧几乎不下降，表示数据质量问题或非生物学峰
   - 正常峰型：左侧明显下降后再上升到峰值
   - **综合判断前检测**：如果 spe 或 num 任一异常，直接判为 peak_shape_abnormal，不进行后续合并和判定
   - **异常峰在合并前被过滤**，不参与后续合并和判定
4. 宽度过滤（当前实现）:
   - 对候选峰从峰顶向两侧查找局部最小值，宽度 = 右谷底位置 - 左谷底位置
   - 宽度 >= min_width（默认15）保留，否则丢弃
5. 肩峰（shoulder）检测（`detect_shoulder=True`，默认开启）：
   - 对平滑曲线求一阶导数并再次 Savitzky-Golay 平滑
   - 找一阶导数的局部极小值（增长速率最慢的位置），即曲线上升段中增长减缓形成的"肩部"
   - 过滤条件：
     - 一阶导数 > 0（确保在上升段，排除下降段的凹陷）
     - 频率 >= 全局最高频率 × `prominence_ratio`（过滤低频噪声）
     - 与已有普通峰的距离 >= `min_distance`（避免重复检测）
   - 通过过滤的肩峰加入普通峰列表，后续参与合并和比值判定
   - 适用场景：人眼可见增长减缓形成的"凸起"，但不是真正的极大值点
6. 返回峰的 depth 列表、frequency 列表、异常标记列表，按升序排列


**比值判定**（以最小峰为基准1，相对误差可配置，默认10%）：

结合判定:

1.SpeFreq.cut的峰数
2.NumFreq.cut的峰数
统计这两个图检测到的总峰数，以及它们的深度、频率，
如果检测到峰的数量为3，判断第三个峰的频率是否在第二个峰频率的70%以上，如果是，则认为是二倍体.

**双文件综合判定实现**：
1. 峰合并逻辑：
   - 将两个文件的峰合并到一个列表，按深度排序
   - 低深度区（depth < 30）：绝对深度差 <= 4 则合并
   - 高深度区（depth >= 30）：深度差 <= 17% * 当前峰深度则合并
   - 合并时：取较大频率值，深度取平均值，来源标记为'both'
2. 总峰数：合并去重后的峰数量
3. 特殊规则（3峰情况）：
   - 检查第3峰depth是否≈2*第1峰depth（容差tolerance范围内），且第3峰频率>=第2峰频率*0.7 → 判定为杂合二倍体
   - 不满足上述条件 → 忽略第3峰，按前2峰常规判定
4. 其他情况：按常规比值判定规则处理
5. 入口函数：`main_dual(spe_filepath, num_filepath, ...)`

| 峰数 | 深度比值 | 判定 |
|------|---------|------|
| 1 | — | 正常，纯合二倍体 |
| 2 | 1:2 | 正常，杂合二倍体 |
| 3 | 1:2:3 | 正常，三倍体 |
| 3 | 1:2:4 | 正常，高杂合二倍体 |
| 4 | 1:2:3:4 | 正常，四倍体 |
| 其他 | — | unknown，暂不处理 |

**实现**：`kmer_judge.py`，入口 `main_dual(spe_filepath, num_filepath, ...)`

**main_dual函数参数说明**：

| 参数 | 默认值 | 说明 |
|------|--------|------|
| `spe_filepath` | 必填 | SpeFreq.cut文件路径 |
| `num_filepath` | 必填 | NumFreq.cut文件路径 |
| `depth_min` | 3 | 有效深度下限，过滤低深度噪声 |
| `depth_max` | 300 | 有效深度上限，截断高深度区域 |
| `smooth_window` | 11 | Savitzky-Golay平滑窗口大小（必须为奇数），越大越平滑 |
| `smooth_poly` | 3 | Savitzky-Golay多项式阶数，控制平滑曲线的拟合灵活度 |
| `prominence_ratio` | 0.05 | 峰高度相对最高峰的最小比例阈值（5%），低于此比例的峰视为噪声过滤掉 |
| `min_distance` | 10 | 相邻峰之间的最小距离（索引单位），防止同一峰被重复检测 |
| `min_width` | 10 | 峰的最小半高全宽（FWHM，索引单位），宽度不足的窄峰被过滤 |
| `tolerance` | 0.2 | 比值匹配的相对误差容忍度（20%），用于判定峰深度比是否符合已知倍性模式 |
| `merge_tolerance` | 0.17 | 高深度区（depth>=30）峰合并时的深度容差（17%），深度差在此范围内的峰合并为同一峰 |
| `merge_low_depth_abs` | 4 | 低深度区（depth<30）峰合并时的绝对深度差阈值，差值<=此值则合并 |
| `merge_low_depth_threshold` | 30 | 低深度区与高深度区的分界深度值 |
| `low_depth_threshold` | 10 | 低深度峰过滤阈值，深度小于此值的第一个峰在不成比例且高度不足时会被忽略 |
| `low_peak_freq_ratio` | 0.6 | 低深度峰高度比例阈值（60%），低深度峰高度不及最高峰此比例时忽略 |
| `spe_left_min_threshold` | 0.33 | SpeFreq 主峰左侧最低点比例阈值（33%），超过此值判为异常 |
| `num_left_min_threshold` | 0.4 | NumFreq 主峰左侧最低点比例阈值（40%），超过此值判为异常 |
| `shoulder_min_freq_ratio` | 0.3 | 肩峰频率最低比例阈值（30%），肩峰频率需达到全局最高峰的此比例才保留，防止低深度噪声区误检 |
| `verbose` | True | 是否打印详细检测过程 |

### NT比对规则（打分制）
all.ntcls 大类文件，每一个样本测序reads比对到 动物、植物、细菌、真菌、病毒的比例
all.ntspe.xls 小类文件 每一个样本测序reads比对到具体物种的比例 只取了top6

1.先看 all.ntcls.xls
第一列是物种名，判断是哪一类生物（五个之一），
1.1 先看top1 是不是该物种所属的生物类别，如果是，则判为pass（2.5分），否则fail（0分）
1.2 细菌、真菌、病毒的比例总和小于本物种比例的10%（若本物种比例<5%则阈值为20%），则判定pass（没有污染，5分），否则fail（0分）

2.再看 all.ntspe.xls
第一列是物种名，判断是哪一类生物（五个之一），
看top6中细菌、真菌、病毒 的比例总和都小于2%，则判定pass（没有污染，2.5分），否则fail（0分）

NT打分等级：
| 总分 | 等级 |
|------|------|
| <4分 | fail |
| 4~6分 | 重度污染 |
| 6~8分 | 轻度污染 |
| >=8分 | 正常 |




### 判定脚本
nohup /data/home/zhurui/.conda/envs/zhurui_agent/bin/python survey_judge.py --max 2 > survey_judge.log 2>&1 &

### 综合判定逻辑

kmer正常时：
| NT等级 | 综合判定 |
|--------|---------|
| 正常/轻度污染 | 正常 |
| 重度污染 | 轻度污染 |
| fail | 重度污染 |

kmer异常时：
| NT等级 | 综合判定 |
|--------|---------|
| 正常 | 重度污染 |
| 其他 | fail |

### 回写字段

| 字段 | 说明 |
|------|------|
| kmer峰型 | 中文峰型名称：纯合二倍体/杂合二倍体/三倍体/高杂合二倍体/四倍体/无峰/未知 |
| kmer是否正常 | 正常/异常 |
| NT大类Top1判断 | pass/fail，Top1类别匹配本物种 |
| NT大类污染判断 | pass/fail，细菌+真菌+病毒比例<本物种比例的10%（本物种<5%时为20%） |
| NT小类污染判断 | pass/fail，Top6中细菌+真菌+病毒比例<2% |
| NT得分 | 数值分数（满分10分） |
| NT等级 | 正常/轻度污染/重度污染/fail |
| 综合判定 | kmer × NT联合判定结果 |



### 优化逻辑
判定结果: unknown
是否正常: 否
详情: 5个峰，depth=[23, 45, 90, 135, 179]，比值=1.00:1.96:3.91:5.87:7.78，不匹配已知模式

当峰大于等于四个的时候，参照如下字典的判断逻辑，如果符合1:2则接着往下看（三倍体和高重复二倍体可以同步看），如果不符合就停止，直到找到最匹配的模式或者列表被裁剪完：

('diploid_hetero',    [1, 2],    '杂合二倍体'),
('triploid',          [1, 2, 3], '三倍体'),
('high_repetitive_diplo', [1, 2, 4], '高重复二倍体'),
('tetraploid',        [1, 2, 3, 4], '四倍体'),


## 单元测试

- 新增文件：`tests/test_kmer_judge.py`
- 验证 `classify_peaks` 的新逻辑：从 `1:2` 方向递进判定，并优先匹配最完整倍性模式。
- 例子：`[23,45,90,135,179]` -> `high_repetitive_diplo`。


## 判定算法
1. kmer峰型异常的结论是如何得出的
在 kmer_judge.py:51-63 的 detect_peaks 函数中：

逻辑是检查每个峰的左侧鞍部（局部最小值）与峰高度的比值：


left_min = freq_smooth[:idx].min()   # 峰左侧所有点的最小值
left_ratio = left_min / peak_freq    # 鞍部占峰高的比例
abnormal_flags.append(left_ratio > 0.9)  # 超过90%标记异常
判定逻辑：

对每个检测到的峰，取其左侧（depth更小方向）平滑曲线的全局最小值 left_min
计算 left_min / peak_freq，即左侧最低点占峰高的比例
如果比例 > 0.9（90%），标记为异常峰型
含义：正常的生物学峰应该是左侧有明显下降再上升到峰值的形态。如果左侧鞍部几乎和峰一样高（>90%），说明这个"峰"其实不是一个真正的凸起，可能是噪声或数据质量问题导致的假峰。

注意：异常峰在合并前被过滤掉，不参与后续的峰合并和 classify_peaks 判定逻辑。

2. 当前如何检测峰的宽度
峰宽度检测在 kmer_judge.py:30-47：


# 新版（当前实现）:
# 从峰顶向左扫描，直到频率不再递减（找左侧局部谷底），
# 从峰顶向右扫描，直到频率不再递减（找右侧局部谷底），
# 宽度 = 右谷底位置 - 左谷底位置。
# 如果宽度 >= min_width（默认15），则保留该峰；否则视为窄峰过滤。

关于参数外置情况：

detect_peaks 函数签名中有 min_width=15 参数
main_dual 函数已经外置了 min_width=15（L184），并传递给 detect_peaks


### v3 优化
2.鞍部检测，从山顶向两边找局部最小值，如果任意一侧鞍部占峰高比例 > 90%，忽略这个峰 暂时别管
3.NT判断，取消top1大于40%，
top1要属于本物种2.5分，细菌真菌病毒，加一起小于本物种的10%才算通过（如果top1比例小于5%，细菌真菌病毒，加一起小于本物种的20%计算通过，5分）
4.Kmer不看小于10的情况:
第一个峰、第二个峰不成比例
若小于10的峰高度不及最高峰的60%，则忽略


### 待优化
[324/405] FDSW250009815-1a_50SPL.DNA
  路径: /data/work/zhurui/survey_rec/data/shenshaoqi_data/survey1/X101SC2510/X101SC25107148-Z04-J001/FDSW250009815-1a_50SPL.DNA

判定结果: high_repetitive_diplo
是否正常: 是
详情: 5个峰，depth=[23, 45, 90]，比值≈1:2:4，高重复二倍体
============================================================
开始NT比对污染判断（打分制）
============================================================


[358/405] FDES250692881-1a_A02.GZ.02
  路径: /data/work/zhurui/survey_rec/data/shenshaoqi_data/survey1/X101SC2512/X101SC251212246-Z03-J001/FDES250692881-1a_A02.GZ.02

判定结果: tetraploid
是否正常: 是
详情: 4个峰，depth=[44, 86, 130, 170]，比值≈1:2:3:4，四倍体
============================================================
开始NT比对污染判断（打分制）
============================================================

【步骤1】检查all.ntcls.xls大类文件
------------------------------------------------------------
目标物种 '羊' 属于: 动物
  物种: 羊
  kmer=tetraploid(正常) | NT: 得分=0 等级=fail | 综合: 重度污染



1.主峰:定为最高峰为主峰，污染峰的阈值设置为主峰深度的20%，即以下规则不再卡固定值10:
第一个峰、第二个峰不成比例
若小于10的峰高度不及最高峰的60%，则忽略


2.两条路线
- 如果合并跨度设置为6
- 两份数据单独判断，都通过，则认为正常，否则维持现有逻辑

[223/405] FDSW250044249-2r_F1SM1.1
  路径: /data/work/zhurui/survey_rec/data/shenshaoqi_data/survey1/X101SC2508/X101SC25086665-Z02-J004/FDSW250044249-2r_F1SM1.1

判定结果: unknown
是否正常: 否
详情: 2个峰，depth=[23, 29]，比值=1.00:1.26，不符合1:2，停止判定（第3峰不符合杂合二倍体模式，忽略）
============================================================
开始NT比对污染判断（打分制）
============================================================

【步骤1】检查all.ntcls.xls大类文件
------------------------------------------------------------
目标物种 '植物' 属于: 植物
各类别比例: {'Plantae': 15.25, 'Metazoa': 3.42, 'Bacteria': 2.98, 'Fungi': 2.34, 'Viruses': 0.56}


测试:去掉左侧鞍部异常峰检测


top6计算方式有问题：
1.当前算的是top3细菌真菌病毒比例之和，应该算top6中细菌真菌病毒比例之和

先不改:
2.大类里面，top1是本物种超过20%的情况，小类里面top6中细菌真菌病毒比例之和要小于大类10%才算通过

kmer优化：
主峰左侧最低点优化:
1. num，要小于 0.4
2. spe，要小于 0.33

要求两个都符合才算正常，否则算异常