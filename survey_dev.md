
## 判定规则

### k-mer图判定规则

**输入**：SpeFreq.cut，有效范围 depth 3~300

**峰检测**：
1. Savitzky-Golay 平滑（window=11，poly=3），消除低深度端噪声和宽峰顶部抖动
2. `scipy.signal.find_peaks` 检测极大值：
   - `prominence ≥ max_freq × 5%`：过滤噪声小峰
   - `distance ≥ 10`：防止同一峰被重复检出
3. 返回峰的 depth 列表，按升序排列

**比值判定**（以最小峰为基准1，相对误差可配置，默认10%）：

| 峰数 | 深度比值 | 判定 |
|------|---------|------|
| 1 | — | 正常，纯合二倍体 |
| 2 | 1:2 | 正常，杂合二倍体 |
| 3 | 1:2:3 | 正常，三倍体 |
| 3 | 1:2:4 | 正常，高杂合二倍体 |
| 4 | 1:2:3:4 | 正常，四倍体 |
| 其他 | — | unknown，暂不处理 |

**实现**：`kmer_judge.py`，入口 `main(filepath, tolerance=0.10)`


